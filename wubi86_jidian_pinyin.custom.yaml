patch:
    # Emoji
    # Reference: ./recipes/emoji/emoji_suggestion.yaml
    __include: recipes/emoji/emoji_suggestion:/patch

    translator/enable_sentence: true # 句子输入模式
    translator/enable_user_dict: true # 是否开启用户词典（用户词典记录动态字词频，用户词）

    reverse_lookup/prefix: "`"
    reverse_lookup/suffix: "'"
    reverse_lookup/tips: 〔拼音〕

    recognizer/patterns/reverse_lookup: "`[a-z]*'?$"
